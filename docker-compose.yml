services:
  veeam-designer:
    image: emb079/veeam-designer:latest
    ports:
      - "8000:8000"
    volumes:
      - ./config.json:/app/config.json
      - ./profiles.json:/app/profiles.json

  # Uncomment below (and comment out the block above) to build from source instead:
  # veeam-designer:
  #   build: .
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./config.json:/app/config.json
  #     - ./profiles.json:/app/profiles.json
